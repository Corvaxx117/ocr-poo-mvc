<?php

/**
 * Vue affichant le formulaire de modification des informations utilisateur.
 */
?>

<div class="edit-profile-container">
    <h2>Modifier mes informations</h2>

    <form method="POST" action="<?= $this->url('/users/updateProfile'); ?>" class="form-style">
        <label for="nickname">Pseudonyme</label>
        <input type="text" name="nickname" id="nickname" value="<?= $this->clean($user['nickname'] ?? '', false); ?>" required>

        <label for="email">Email</label>
        <input type="email" name="email" id="email" value="<?= $this->clean($user['email'] ?? '', false); ?>" required>

        <label for="password">Mot de passe</label>
        <input type="password" name="password" id="password" required>

        <label for="confirm_password">Confirmer le mot de passe</label>
        <input type="password" name="confirm_password" id="confirm_password" required>

        <button type="submit" class="btn btn-save">💾 Enregistrer</button>
        <a href="<?= $this->url('/users/profile'); ?>" class="btn btn-cancel">❌ Annuler</a>
    </form>
</div>